<div class="flex flex-col ml-2 border-l-2 pl-2 py-6">
  <div class="flex flex-row -ml-[1.30rem] space-x-3 items-center">
    <div
      class="bg-gray-100 rounded-full flex items-center justify-center w-6 h-6 aspect-square"
    >
      <slot name="icon" />
    </div>
    <div class="text-xl">
      <slot name="title" />
    </div>
  </div>

  <div class="flex flex-row ml-[0.95rem]">
    <slot />
  </div>

  <div class="flex flex-row space-x-2 ml-5">
    <slot name="footer" />
  </div>
</div>
